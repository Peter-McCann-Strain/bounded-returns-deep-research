# Tested direct dependency versions for the public release.
#
# Install with:
#   python -m pip install -c constraints-public.txt -e ".[api,paper,dev]"
#
# These are the versions the shipped results were produced and re-verified with.
# GPU/local-model packages (the ".[local]" extra) are listed at the bottom but
# are NOT pinned here: torch must match your CUDA build, and pinning it would
# break `pip install -c` on machines with a different accelerator.

# Core
httpx==0.28.1
pydantic==2.12.5
python-dotenv==1.2.2
structlog==25.5.0

# .[api] — providers, retrieval, extraction
aiohttp==3.13.3
aiolimiter==1.2.1
anthropic==0.120.2
arxiv==2.4.1
beautifulsoup4==4.14.3
openai==2.24.0
requests==2.32.5
tavily-python==0.7.22
trafilatura==2.0.0

# .[paper] — statistics and figures
bibtexparser==1.4.4
krippendorff==0.8.2
matplotlib==3.10.8
numpy==1.26.4
pandas==2.3.3
patsy==1.0.2
pingouin==0.6.1
pyarrow==23.0.1
scikit-learn==1.8.0
scikit-posthocs==0.12.0
scipy==1.12.0
seaborn==0.13.2
statsmodels==0.14.6

# .[dev]
pytest==9.0.2
pytest-asyncio==1.3.0
ruff==0.16.1

# .[local] — reference versions only, intentionally NOT pinned above.
# Install a CUDA-matched torch first, then: pip install -e ".[local]"
# Reference environment: torch==2.10.0, transformers==5.2.0, accelerate==1.13.0,
# bitsandbytes==0.49.2, peft==0.19.1, trl==1.3.0, datasets==4.8.5
